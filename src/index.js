import { renderContacts } from "./contact-page";
import {
  renderTopSection,
  renderMiddleSection,
  renderFoofer,
} from "./initial-page";
import { renderMenu } from "./menu-page";
import "./style.css";

renderTopSection();
renderMiddleSection();
renderFoofer();

renderMenu();

renderContacts();
